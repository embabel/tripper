spring:

  ai:
    mcp:
      client:
        enabled: true
        name: embabel
        version: 1.0.0
        request-timeout: 30s
        type: SYNC

        sse:
          connections:
            mcp-gateway:
              url: http://localhost:9011

